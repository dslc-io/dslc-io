---
title: "Page Not Found"
format:
  html:
    include-in-header:
      - text: |
          <script type="text/javascript">
            var redirects = "redirects.tsv";
            var current_path = window.location.pathname;
            var query = window.location.search;
            var hash = window.location.hash;
            var new_url = "";
            var xhr = new XMLHttpRequest();
            xhr.open("GET", redirects, false);
            xhr.send();
            var lines = xhr.responseText.split("\n");
            for (var i = 0; i < lines.length; i++) {
              var parts = lines[i].split("\t");
              if (parts[0] == current_path) {
                window.location.href = parts[1] + query + hash;
                break;
              }
            }
          </script>
resources:
  - "redirects.tsv"
---

We cannot find that page.
If you thought there would be a redirect here, try using "r4ds.io" in place of "dslc.io".
We'll get the redirects set up eventually!
